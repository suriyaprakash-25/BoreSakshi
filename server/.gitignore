node_modules/
data.json
feature-artifacts/
rig-media/

# local/raw geospatial datasets are large and may have redistribution restrictions
# keep source rasters/vectors outside git; commit only manifests/docs/tests.
data/
geospatial-data/

# secrets — never commit real environment values
.env
.env.local
.env.*.local
